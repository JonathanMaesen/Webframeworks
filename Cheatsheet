
Slow start
    npm create vite@latest

Quick start
npm create vite@latest our-first-react-app -- --template react-ts

Start Program:
    npm run dev

Project met Styling:
npx degit similonap/vite-template-react-shadcn react-vite-shadcn

Snell react ding:
npx create-webftemplate my-app

INPUT VELD = STARE ALWAYS

INPUT VELD = STATE ALWAYS
changing states: Always take copy, & then pass copy over to set<nameOfState>

{boolean && ()} <= conditional rendering veel eenvoudige vorm van if statement conditional rendering
Effects are just code that runs after the dom loads
bij te studeren:
    Maps
    reduce
    sort
    find