# create-webftemplate

A tiny CLI to scaffold a fresh **Vite + React** app from your `webftemplate`.

## Usage

```bash
npm create webftemplate@latest my-app
# or
npx create-webftemplate my-app
```

The CLI will:
- copy the `template/` directory into `my-app`
- set `package.json` name to `my-app`